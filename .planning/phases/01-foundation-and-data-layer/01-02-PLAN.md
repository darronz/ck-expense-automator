---
phase: 01-foundation-and-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/types.ts
  - lib/rules-store.ts
  - lib/expense-engine.ts
  - lib/vendor-extractor.ts
  - entrypoints/background.ts
  - tests/rules-store.test.ts
  - tests/expense-engine.test.ts
  - tests/vendor-extractor.test.ts
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-06
  - RULE-07
  - RULE-08
must_haves:
  truths:
    - "Rules written to chrome.storage.sync survive a get() roundtrip with all fields intact"
    - "Default rules (Virgin Media, Supabase, LinkedIn, etc.) are seeded on first install via onInstalled"
    - "matchRule returns true for a description that matches the rule's pattern, false otherwise"
    - "validateVat rejects VAT exceeding 20% of net amount and accepts valid amounts"
    - "buildPayload produces a URLSearchParams with all 20 required CK form fields including ASP.NET double-checkbox fields"
    - "extractVendor correctly parses ONLINE PAYMENT, DIRECT DEBIT, CARD SUBSCRIPTION, and APPLE PAY description lines"
    - "recordRuleUsage increments matchCount and sets lastUsed in chrome.storage.local (not sync)"
    - "All tests pass: npx vitest run exits 0"
  artifacts:
    - path: "lib/types.ts"
      provides: "ExpenseRule, RulesConfig, RuleStats, ExpenseSubmission TypeScript interfaces"
      contains: "ExpenseRule"
    - path: "lib/rules-store.ts"
      provides: "chrome.storage.sync CRUD: getRules, saveRules, addRule, updateRule, deleteRule, recordRuleUsage"
      exports: ["getRules", "saveRules", "addRule", "updateRule", "deleteRule", "recordRuleUsage", "DEFAULT_RULES"]
    - path: "lib/expense-engine.ts"
      provides: "matchRule, validateVat, buildPayload, calculateVatFromPercentage"
      exports: ["matchRule", "validateVat", "buildPayload", "matchExpenses"]
    - path: "lib/vendor-extractor.ts"
      provides: "extractVendor from Starling bank description strings"
      exports: ["extractVendor"]
    - path: "entrypoints/background.ts"
      provides: "onInstalled listener that seeds DEFAULT_RULES on first install"
      contains: "onInstalled"
    - path: "tests/rules-store.test.ts"
      provides: "Unit tests for RULE-01, RULE-02, RULE-03, RULE-08"
      contains: "recordRuleUsage"
    - path: "tests/expense-engine.test.ts"
      provides: "Unit tests for RULE-04, RULE-05, RULE-06"
      contains: "validateVat"
    - path: "tests/vendor-extractor.test.ts"
      provides: "Unit tests for RULE-07"
      contains: "extractVendor"
  key_links:
    - from: "entrypoints/background.ts"
      to: "lib/rules-store.ts DEFAULT_RULES"
      via: "import and browser.storage.sync.set on onInstalled"
      pattern: "DEFAULT_RULES"
    - from: "lib/expense-engine.ts"
      to: "lib/types.ts ExpenseRule"
      via: "TypeScript import"
      pattern: "import.*ExpenseRule.*from"
    - from: "lib/rules-store.ts"
      to: "browser.storage.sync"
      via: "browser.storage.sync.get/set"
      pattern: "browser\\.storage\\.sync"
    - from: "lib/rules-store.ts recordRuleUsage"
      to: "browser.storage.local"
      via: "browser.storage.local.get/set (not sync)"
      pattern: "browser\\.storage\\.local"
---

<objective>
Implement the rule storage layer, core expense matching engine, and smart vendor extractor — the pure TypeScript logic modules that power everything in Phases 2-4. All logic is covered by unit tests using WXT's fake-browser.

Purpose: These modules are the data layer. The panel UI (Phase 3) and portal interaction (Phase 2) both depend on these functions being correct and tested before they are called.
Output: lib/types.ts, lib/rules-store.ts, lib/expense-engine.ts, lib/vendor-extractor.ts, updated background.ts, and three passing test suites.
</objective>

<execution_context>
@/Users/darron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Key constants and schema from CLAUDE.md for the executor -->

ExpenseRule interface (from CLAUDE.md):
```typescript
interface ExpenseRule {
  id: string;                    // UUID
  name: string;                  // Human-readable name
  matchPattern: string;          // Regex pattern
  matchFlags?: string;           // Default: 'i'
  nominalId: string;             // NominalId category ID
  description: string;           // Reason for expense
  purchasedFrom: string;         // Vendor name
  hasVat: boolean;
  vatAmount: number | null;      // Fixed VAT, or null
  vatPercentage: number | null;  // VAT as % of gross (alternative to fixed)
  enabled: boolean;
  createdAt: string;             // ISO date
  lastUsed?: string;
  matchCount?: number;
}
```

Required CK form fields (all 20, from CLAUDE.md):
Id, ExpenseClaimId, AccountingTypeId, ExpenseDates (dd/mm/yyyy), FirstExpenseDate (yyyy-mm-dd),
LastExpenseDate (yyyy-mm-dd), VisibleDate (empty), NominalId, Description, PurchasedFrom,
ExpensePaymentTypeId (always "2"), ActiveUserCompanyFrsRegistered (always "False"),
ActiveUserCompanyVatRegistered (always "True"), GrossAmountPaid, HasVatReceipt (double-field),
VatAmountPaid, NetAmountPaid (Gross - VAT), MappedSuspenseItemIds, IsHavingSuspenseItems (always "True"),
IsMappedToSuspenseItems (double-field), PreviousPage

NominalId values: 48=Telephone, 50=Stationery, 51=Advertising, 52=Travel, 53=Subsistence,
55=Fuel, 61=Licenses, 62=Insurance, 64=Maintenance, 65=Staff training, 68=Subscriptions,
69=Office equipment, 70=Computer peripherals, 72=Consultancy, 74=Books, 78=Legal/Professional,
81=Entertainment, 83=Promotional, 85=Bank charges, 114=Computer equipment

Bank description format (Starling):
Line 1: "Ref: Starling Account: XXXXXXXX, SortCode: XX-XX-XX"
Line 2+: "ONLINE PAYMENT SUPABASE $25.00, Rate: 1.3390"
      or "DIRECT DEBIT Virgin Media 760869601001"
      or "CARD SUBSCRIPTION LinkedInPreA 70807384"
      or "APPLE PAY Apple 123456"

VAT validation rule: VAT must be > 0 AND ≤ (gross - vat) * 0.20
VAT from percentage: net = gross / (1 + vatPct/100); vat = gross - net
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test scaffolding (Wave 0) — all three test suites</name>
  <read_first>
    - /Users/darron/Work/ck-expense-automator/.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md (Validation Architecture section, Wave 0 requirements, code examples)
    - /Users/darron/Work/ck-expense-automator/.planning/phases/01-foundation-and-data-layer/01-VALIDATION.md (per-task verification map, Wave 0 checklist)
    - /Users/darron/Work/ck-expense-automator/CLAUDE.md (ExpenseRule schema, DEFAULT_RULES, bank description formats, VAT rule)
    - /Users/darron/Work/ck-expense-automator/vitest.config.ts (if it exists — confirm WxtVitest plugin is present)
  </read_first>
  <files>tests/rules-store.test.ts, tests/expense-engine.test.ts, tests/vendor-extractor.test.ts</files>
  <behavior>
    rules-store.test.ts:
    - getRules() on empty storage returns []
    - saveRules() + getRules() roundtrip preserves all ExpenseRule fields including id, matchPattern, nominalId, hasVat, vatAmount, vatPercentage
    - addRule() appends to existing array, not replaces it
    - updateRule() replaces the rule with matching id, leaves others unchanged
    - deleteRule() removes rule with matching id, leaves others unchanged
    - DEFAULT_RULES is an array with at least 8 items
    - Each DEFAULT_RULE has: id (string), name (string), matchPattern (string), nominalId (string), enabled: true
    - recordRuleUsage() increments matchCount by 1 and sets lastUsed to a non-null ISO string
    - recordRuleUsage() uses browser.storage.local (not sync) — key is "stats-{ruleId}"

    expense-engine.test.ts:
    - matchRule() returns true when description matches rule.matchPattern (case-insensitive by default)
    - matchRule() returns false when description does not match
    - matchRule() returns false when rule.enabled is false (even if pattern matches)
    - matchRule() returns false (does not throw) when rule.matchPattern is an invalid regex like "["
    - validateVat(72, 12) returns { valid: true } (£12 VAT on £72 gross = 20% of £60 net, exactly at limit)
    - validateVat(72, 12.01) returns { valid: false } (exceeds 20% of net)
    - validateVat(72, 0) returns { valid: false } (VAT must be > 0)
    - validateVat(72, -1) returns { valid: false }
    - buildPayload returns URLSearchParams containing all of: Id, ExpenseClaimId, AccountingTypeId, ExpenseDates, FirstExpenseDate, LastExpenseDate, VisibleDate, NominalId, Description, PurchasedFrom, ExpensePaymentTypeId, ActiveUserCompanyFrsRegistered, ActiveUserCompanyVatRegistered, GrossAmountPaid, HasVatReceipt, VatAmountPaid, NetAmountPaid, MappedSuspenseItemIds, IsHavingSuspenseItems, IsMappedToSuspenseItems, PreviousPage
    - buildPayload sets HasVatReceipt to "true" when hasVat is true
    - buildPayload always appends a second HasVatReceipt with value "false" (ASP.NET double-field)
    - buildPayload sets NetAmountPaid to gross - vat (string representation)
    - calculateVatFromPercentage(72, 20) returns 12 (not 14.4)
    - matchExpenses() returns matched items with the first matching rule and unmatched items separately

    vendor-extractor.test.ts:
    - extractVendor("Ref: Starling Account: 12345678, SortCode: 00-00-00\nONLINE PAYMENT SUPABASE $25.00, Rate: 1.3390") returns "SUPABASE" (or normalised equivalent)
    - extractVendor("Ref: Starling...\nDIRECT DEBIT Virgin Media 760869601001") returns "Virgin Media" (trailing numbers stripped)
    - extractVendor("Ref: Starling...\nCARD SUBSCRIPTION LinkedInPreA 70807384") returns "LinkedInPreA" (trailing numbers stripped)
    - extractVendor("Ref: Starling...\nAPPLE PAY Apple 123456") returns "Apple" (trailing numbers stripped)
    - extractVendor("Ref: Starling...\nSOMETHING UNRECOGNISED") returns null
  </behavior>
  <action>
Create the three test files. These are the failing tests (RED phase) — the implementation modules do not exist yet. The tests import from relative paths that will exist after Task 2.

tests/rules-store.test.ts — import from '../lib/rules-store'. Use WxtVitest fake-browser — no manual mock setup needed; the WxtVitest plugin in vitest.config.ts provides the in-memory storage implementation automatically.

tests/expense-engine.test.ts — import from '../lib/expense-engine'. All functions are pure (no browser APIs), so no special setup needed beyond standard Vitest.

tests/vendor-extractor.test.ts — import from '../lib/vendor-extractor'. Pure function, no setup needed.

All test files use Vitest `describe`/`it`/`expect` globals (already configured by vitest.config.ts `globals: true`).

After creating the test files, run `npx vitest run` — it MUST fail with module-not-found errors (because lib/ modules don't exist yet). This is the expected RED state. If tests pass before implementation exists, the tests are wrong.
  </action>
  <verify>
    <automated>cd /Users/darron/Work/ck-expense-automator &amp;&amp; npx vitest run 2>&amp;1 | grep -E "FAIL|Cannot find module|not found" | head -5 &amp;&amp; echo "RED state confirmed (tests fail before implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rules-store.test.ts` exists and contains "recordRuleUsage"
    - `tests/expense-engine.test.ts` exists and contains "validateVat"
    - `tests/vendor-extractor.test.ts` exists and contains "extractVendor"
    - `npx vitest run` exits non-zero (tests failing = RED state — implementation not yet written)
    - Test files do NOT import from files that already exist with working implementations
  </acceptance_criteria>
  <done>Three test files exist in RED state. All imports reference lib/ modules that don't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement types.ts, rules-store.ts, and background onInstalled</name>
  <read_first>
    - /Users/darron/Work/ck-expense-automator/tests/rules-store.test.ts (what functions and shapes the tests expect — this is the contract to implement against)
    - /Users/darron/Work/ck-expense-automator/.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md (Pattern 3: rules-store.ts, Pattern 4: Background onInstalled, Pitfall 4: storage quota, Anti-pattern: stats in sync, DEFAULT_RULES from code examples section)
    - /Users/darron/Work/ck-expense-automator/CLAUDE.md (DEFAULT_RULES array, ExpenseRule schema, RULE-08 stats requirement)
    - /Users/darron/Work/ck-expense-automator/entrypoints/background.ts (current stub to replace)
  </read_first>
  <files>lib/types.ts, lib/rules-store.ts, entrypoints/background.ts</files>
  <behavior>
    - See tests/rules-store.test.ts for exact expected behavior
    - Key: recordRuleUsage writes to browser.storage.local with key "stats-{ruleId}", never to storage.sync
    - Key: saveRules should include a byte-count guard: console.warn at 25 rules, throw descriptive error at 35 rules
    - Key: DEFAULT_RULES must include all 8 rules from CLAUDE.md (Virgin Media, Supabase, LinkedIn, Claude AI, Apple Developer, Three Mobile, iCloud+, Twilio) each with a generated UUID (use crypto.randomUUID() at module init time)
  </behavior>
  <action>
Create `lib/types.ts` first with all shared TypeScript interfaces:

```typescript
// lib/types.ts

export interface ExpenseRule {
  id: string;
  name: string;
  matchPattern: string;
  matchFlags?: string;
  nominalId: string;
  description: string;
  purchasedFrom: string;
  hasVat: boolean;
  vatAmount: number | null;
  vatPercentage: number | null;
  enabled: boolean;
  createdAt: string;
  lastUsed?: string;
  matchCount?: number;
}

export interface RulesConfig {
  rules: ExpenseRule[];
  version: number;
}

export interface RuleStats {
  matchCount: number;
  lastUsed: string | null;
}

export interface ExpenseSubmission {
  claimId: string;
  suspenseItemId: string;
  date: string;        // dd/mm/yyyy
  isoDate: string;     // yyyy-mm-dd
  nominalId: string;
  description: string;
  purchasedFrom: string;
  grossAmount: number;
  hasVat: boolean;
  vatAmount: number | null;
  previousPage: string;
}

export interface MatchResult {
  matched: Array<{ item: SuspenseItem; rule: ExpenseRule }>;
  unmatched: SuspenseItem[];
}

export interface SuspenseItem {
  id: string;
  date: string;        // dd/mm/yyyy from DataTable
  isoDate: string;     // yyyy-mm-dd derived
  description: string;
  amount: number;
}
```

Create `lib/rules-store.ts` implementing all CRUD functions plus DEFAULT_RULES. Use the patterns from RESEARCH.md Pattern 3 and Pattern 4. Key implementation details:

- STORAGE_KEY = 'rules' (single key in storage.sync)
- STATS_KEY_PREFIX = 'stats-' (in storage.local)
- DEFAULT_RULES array: all 8 rules from CLAUDE.md with `crypto.randomUUID()` for each id and `new Date().toISOString()` for createdAt — called once at module load time
- saveRules(): before writing, check `JSON.stringify(rules).length`; if > 5000, console.warn; if > 8000, throw new Error('Rules storage limit approaching — delete some rules before adding more')
- recordRuleUsage(ruleId): reads from browser.storage.local, increments matchCount, sets lastUsed to new Date().toISOString(), writes back to browser.storage.local

Update `entrypoints/background.ts` to import DEFAULT_RULES from lib/rules-store and seed them on install:

```typescript
import { DEFAULT_RULES } from '../lib/rules-store';

export default defineBackground(() => {
  browser.runtime.onInstalled.addListener(async (details) => {
    if (details.reason === 'install') {
      const existing = await browser.storage.sync.get('rules');
      if (!existing['rules']) {
        await browser.storage.sync.set({ rules: DEFAULT_RULES });
        console.log('[CK Expense Automator] Default rules seeded:', DEFAULT_RULES.length);
      }
    }
  });
});
```

After implementation, run `npx vitest run tests/rules-store.test.ts` — all tests in this file must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/darron/Work/ck-expense-automator &amp;&amp; npx vitest run tests/rules-store.test.ts --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/rules-store.test.ts` exits 0 (all tests GREEN)
    - `lib/types.ts` exports: ExpenseRule, RulesConfig, RuleStats, ExpenseSubmission, MatchResult, SuspenseItem
    - `lib/rules-store.ts` exports: getRules, saveRules, addRule, updateRule, deleteRule, recordRuleUsage, DEFAULT_RULES
    - `lib/rules-store.ts` contains "browser.storage.local" (for stats) and "browser.storage.sync" (for rules) — both present
    - `lib/rules-store.ts` contains "stats-" (the storage.local key prefix for stats)
    - DEFAULT_RULES has exactly 8 entries: grep returns count 8 for `name:` occurrences in the array
    - `entrypoints/background.ts` contains "onInstalled" and "DEFAULT_RULES"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>rules-store.ts test suite passes. Types defined. DEFAULT_RULES seeded on install. Stats in storage.local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement expense-engine.ts and vendor-extractor.ts</name>
  <read_first>
    - /Users/darron/Work/ck-expense-automator/tests/expense-engine.test.ts (exact expected behavior for matchRule, validateVat, buildPayload, calculateVatFromPercentage, matchExpenses)
    - /Users/darron/Work/ck-expense-automator/tests/vendor-extractor.test.ts (exact expected behavior for extractVendor)
    - /Users/darron/Work/ck-expense-automator/.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md (Pattern 5: VAT validation, Pattern 6: Form payload builder, Pattern 7: Smart vendor extraction, Pitfall 3: VAT percentage calculation, Pitfall 6: regex errors)
    - /Users/darron/Work/ck-expense-automator/CLAUDE.md (all 20 form fields table, ASP.NET double-checkbox pattern, VAT validation rule, bank description formats)
    - /Users/darron/Work/ck-expense-automator/lib/types.ts (ExpenseRule, ExpenseSubmission, MatchResult, SuspenseItem interfaces)
  </read_first>
  <files>lib/expense-engine.ts, lib/vendor-extractor.ts</files>
  <behavior>
    - See tests/expense-engine.test.ts and tests/vendor-extractor.test.ts for exact expected behavior
    - Critical: calculateVatFromPercentage(72, 20) must return 12 not 14.4 (formula: net = gross / (1 + pct/100))
    - Critical: buildPayload must append HasVatReceipt twice (ASP.NET double-field) and IsMappedToSuspenseItems twice
    - Critical: matchRule must catch SyntaxError from invalid regex and return false instead of throwing
    - Critical: extractVendor must strip trailing reference numbers (6+ digit sequences at end of vendor string)
  </behavior>
  <action>
Create `lib/expense-engine.ts` implementing the four exported functions. Implement against the test contracts in tests/expense-engine.test.ts.

Key implementation notes for buildPayload (from CLAUDE.md "Form Fields" table):
- `Id` always `'0'`
- `AccountingTypeId` always `'2'`
- `ExpensePaymentTypeId` always `'2'` (Business account)
- `ActiveUserCompanyFrsRegistered` always `'False'`
- `ActiveUserCompanyVatRegistered` always `'True'`
- `IsHavingSuspenseItems` always `'True'`
- `HasVatReceipt` — append TWICE: first the actual value, then `'false'` (ASP.NET pattern)
- `IsMappedToSuspenseItems` — append TWICE: first `'true'`, then `'false'`
- `NetAmountPaid` = `(grossAmount - (vatAmount ?? 0)).toFixed(2)` converted to string
- `VatAmountPaid` = `(vatAmount ?? 0).toFixed(2)` converted to string
- `GrossAmountPaid` = `grossAmount.toFixed(2)` — preserve two decimal places

matchExpenses(items: SuspenseItem[], rules: ExpenseRule[]): MatchResult — iterates items, for each tries rules in array order, returns first match. No rule match = unmatched.

Create `lib/vendor-extractor.ts` implementing extractVendor(description: string): string | null.

Patterns (from RESEARCH.md Pattern 7):
```
ONLINE PAYMENT {VENDOR} [$amount, Rate: ...]
DIRECT DEBIT {VENDOR} [reference-number]
CARD SUBSCRIPTION {VENDOR} [reference-number]
APPLE PAY {VENDOR} [reference-number]
```

The description has a "Ref: Starling Account..." prefix line — strip it first (slice from line 2 onwards). Apply patterns against the remaining content. Normalise: strip trailing sequences of 6+ digits, trim whitespace.

After implementation, run the full test suite:
```
npx vitest run
```
All three test files must pass (GREEN). Then run `npx tsc --noEmit` for TypeScript clean check.
  </action>
  <verify>
    <automated>cd /Users/darron/Work/ck-expense-automator &amp;&amp; npx vitest run --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 — all tests in rules-store.test.ts, expense-engine.test.ts, and vendor-extractor.test.ts pass
    - `lib/expense-engine.ts` exports: matchRule, validateVat, buildPayload, calculateVatFromPercentage, matchExpenses
    - `lib/vendor-extractor.ts` exports: extractVendor
    - `lib/expense-engine.ts` contains two `form.append('HasVatReceipt'` calls (ASP.NET double-field)
    - `lib/expense-engine.ts` contains two `form.append('IsMappedToSuspenseItems'` calls
    - `lib/expense-engine.ts` contains a try/catch around `new RegExp(` (Pitfall 6 guard)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three test suites pass. expense-engine.ts and vendor-extractor.ts are TypeScript-clean. Full test suite GREEN.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 — all unit tests GREEN
- `npx tsc --noEmit` exits 0 — no TypeScript errors
- lib/types.ts: exports ExpenseRule, ExpenseSubmission, SuspenseItem, MatchResult
- lib/rules-store.ts: uses browser.storage.sync for rules, browser.storage.local for stats
- lib/expense-engine.ts: buildPayload has 20 fields including double-field HasVatReceipt and IsMappedToSuspenseItems
- lib/vendor-extractor.ts: handles all 4 description patterns from CLAUDE.md
- entrypoints/background.ts: seeds DEFAULT_RULES on details.reason === 'install'
</verification>

<success_criteria>
- All 8 Phase 1 RULE-* requirements covered by passing unit tests
- No browser.storage calls in lib/expense-engine.ts or lib/vendor-extractor.ts (pure functions)
- No browser.storage.sync calls in recordRuleUsage (must use local)
- DEFAULT_RULES contains all 8 rules from CLAUDE.md with valid UUIDs
- VAT percentage calculation formula: net = gross / (1 + pct/100), NOT gross * pct/100
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-02-SUMMARY.md` following the summary template.
</output>
